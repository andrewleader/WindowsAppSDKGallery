<UserControl
    x:Class="WindowsAppSDKGallery.Controls.ObjectControlHelpers.ExceptionObjectControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsAppSDKGallery.Controls.ObjectControlHelpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid CornerRadius="4" Background="{ThemeResource AppBarItemBackgroundThemeBrush}" BorderBrush="{ThemeResource ButtonBorderThemeBrush}" BorderThickness="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="4">
            <TextBlock
                x:Name="TextBlockExceptionType"
                Style="{ThemeResource CaptionTextBlockStyle}"
                FontWeight="Bold"/>
            <TextBlock
                x:Name="TextBlockExceptionDetails"
                Style="{ThemeResource CaptionTextBlockStyle}"
                TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
